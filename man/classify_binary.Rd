% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_binary}
\alias{classify_binary}
\title{Classify biomarker into binary categories using a single threshold}
\usage{
classify_binary(
  x,
  cutpoint,
  levels = c("negative", "positive"),
  loq = c(-Inf, Inf)
)
}
\arguments{
\item{x}{Numeric vector of biomarker values.}

\item{cutpoint}{Single numeric cut-point separating two categories.}

\item{levels}{Character vector of length 2, specifying labels for values
below and above the threshold, respectively. Defaults to \code{c("negative", "positive")}.}

\item{loq}{Numeric vector of length 2. Limits of quantification: lower and
upper limits. Defaults to \code{c(-Inf, Inf)} for no limits.}
}
\value{
A factor with two levels, with NA for values outside the quantifiable range.
}
\description{
\code{\link[=classify_binary]{classify_binary()}} function assigns values to two categories based on a
single numeric threshold. Values below the threshold receive the first label
in \code{levels}, and values equal to or above the threshold receive the second
label.

Values below the lower limit of quantification (\code{lloq}) or above the upper
limit of quantification (\code{uloq}) are returned as \code{NA}.
}
\seealso{
\itemize{
\item \code{\link[=classify_ternary]{classify_ternary()}} for two cut-point categorization, allowing for an indeterminant class.
\item \code{\link[=classify]{classify()}} for more flexibility.
}
}
