% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/and.R
\name{and}
\alias{and}
\title{Kleene logical AND: strong or weak}
\usage{
and(x, y, kleene_logic = c("strong", "weak"))
}
\arguments{
\item{x, y}{Logical vectors (will be recycled if lengths differ).}

\item{kleene_logic}{One of "strong" (default) or "weak".
\itemize{
\item "strong": R’s default three-valued AND; FALSE dominates.
\item "weak": propagate NA whenever either operand is NA.
}}
}
\value{
A logical vector of length max(length(x), length(y)).
}
\description{
Kleene logical AND: strong or weak
}
\examples{
# And operation with strong Kleene logic (default), same as `&`.
and(c(TRUE, FALSE, FALSE, NA), c(TRUE, TRUE, NA, NA))

# Weak Kleene logic (propagate `NA` even if other is `FALSE`).
and(c(TRUE, FALSE, FALSE, NA), c(TRUE, TRUE, NA, NA), kleene_logic = "weak")

}
