% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_ternary}
\alias{classify_ternary}
\title{Classify biomarker into ternary categories using two thresholds}
\usage{
classify_ternary(
  x,
  cutpoints,
  levels = c("negative", "indeterminant", "positive"),
  loq = c(lloq = -Inf, uloq = Inf)
)
}
\arguments{
\item{x}{Numeric vector of biomarker values.}

\item{cutpoints}{Numeric vector of cut-off points, sorted ascending.}

\item{levels}{Character vector of length 3. Default is \code{c("negative", "indeterminant", "positive")}.}

\item{loq}{Numeric vector of length 2. Limits of quantification: lower and
upper limits. Defaults to \code{c(-Inf, Inf)} for no limits.}
}
\value{
A factor with three levels, with \code{NA} for values outside the
quantifiable range.
}
\description{
This function assigns values to three ordered categories using two numeric
thresholds. The default classification is:
\itemize{
\item Values strictly below lowest of \code{cutpoints} → \code{"negative"}
\item Values between \code{cutpoints} values → \code{"indeterminant"}
\item Values equal to or above highest of \code{cutpoints} → \code{"positive"}
}

Interval boundaries are right-closed. Values outside the quantifiable range
i.e. below or above \code{loq} values are returned as \code{NA}.
}
\seealso{
\itemize{
\item \code{\link[=classify_binary]{classify_binary()}} for single cut-point categorization.
\item \code{\link[=classify]{classify()}} for more flexibility.
}
}
